# Direct dependencies of the terrain worker (worker.py + renderer.py + the
# remoulade CLI run_worker.sh launches). download_cuzk.py stays stdlib-only.
#
# requirements.txt is the compiled, hash-pinned lock — regenerate after edits:
#   uv pip compile --generate-hashes --python-version 3.12 \
#       requirements.in -o requirements.txt
numpy==2.4.4
pillow==12.2.0
psutil==7.2.2
rasterio
remoulade[rabbitmq,limits]==7.0.0   # extras: broker.py needs limits, RabbitmqBroker needs amqpstorm (cf. api/pyproject.toml)
requests==2.34.2
