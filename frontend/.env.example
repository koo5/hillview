VITE_FEATURE_USER_ACCOUNTS=true

# Enables dev-only features (e.g. internal/experimental tile providers in the map picker).
# Also selects the dev OAuth deep-link scheme (cz.hillviedev) for the Tauri/Android app.
# Android build scripts set this via shell env, which overrides this file.
VITE_DEV_MODE=false

ANDROID_HOME=/home/user/Android/Sdk
VITE_BACKEND=http://localhost:8055/api

# Server-side URL used during SSR to reach the api.
# Bridge mode: http://api:8055/api   Host mode / dev: http://localhost:8055/api
BACKEND_INTERNAL_URL=http://localhost:8055/api

# Debounce window (ms) for per-photo detail fetches in the gallery (ratings/likes, flag status).
# Suppresses request bursts when rapidly swiping through photos. Default: 250
VITE_PHOTO_DETAIL_FETCH_DEBOUNCE_MS=250

# Umami Analytics (set VITE_UMAMI_WEBSITE_ID after creating a website in Umami dashboard at localhost:3333)
VITE_UMAMI_WEBSITE_ID=
VITE_UMAMI_URL=http://localhost:3344
