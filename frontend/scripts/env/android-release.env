# Android release specific environment variables
# Fish syntax - source this after android-base.env for release builds

set -gx VITE_DEV_MODE false

# Production backend URL
set -gx VITE_BACKEND_ANDROID https://api.hillview.app