# Android debug/development specific environment variables
# Fish syntax - includes android-base.env automatically

# First load base Android SDK configuration
source (dirname (status --current-filename))/android-base.env

set -gx VITE_DEV_MODE true

# Default to emulator backend, but allow override for real devices
# Override with: set VITE_BACKEND_ANDROID http://192.168.1.100:8055/api; and ./script.sh
set -q VITE_BACKEND_ANDROID; or set -gx VITE_BACKEND_ANDROID http://10.0.2.2:8055/api

set -gx DEV_MODE true
