<script lang="ts">
	import { browser } from '$app/environment';
	import { page } from '$app/stores';
	import { HILLVIEW_BASE_URL } from '$lib/urlUtilsServer';
	import PhotoHead from '$lib/components/PhotoHead.svelte';
	import JsonLd from '$lib/components/JsonLd.svelte';
	import {
		buildHeadTitle,
		buildHeadDescription,
		buildPhotoImageJsonLd,
		pickOgImage,
		type PublicPhoto,
		type PhotoAnnotation
	} from '$lib/photoDisplay';

	export let data:
		| { photo?: PublicPhoto | null; annotations?: PhotoAnnotation[] }
		| undefined = undefined;

	let Main: any;
	if (browser && typeof window !== 'undefined') {
		async function loadMain() {
			console.log('Loading Main component dynamically');
			const module = await import('$lib/components/Main.svelte');
			Main = module.default;
		}
		loadMain();
	}

	// When a ?photo= share card was resolved server-side, render the same
	// OpenGraph + JSON-LD head tags as the /photo/[uid] detail route (shared
	// helpers, same PublicPhoto shape). Otherwise fall back to the site card.
	$: photo = data?.photo ?? null;
	$: annotations = data?.annotations ?? [];
	$: ogImage = photo ? pickOgImage(photo) : null;
</script>

{#if photo}
	<PhotoHead
		title={buildHeadTitle(photo)}
		description={buildHeadDescription(photo, annotations)}
		ogType="article"
		ogUrl={$page.url.href}
		ogImage={ogImage ? { url: ogImage.url, width: ogImage.width, height: ogImage.height } : null}
		latitude={photo.latitude}
		longitude={photo.longitude}
		canonicalUrl={`${HILLVIEW_BASE_URL}/photo/${encodeURIComponent(photo.uid)}`}
	/>
	<JsonLd data={buildPhotoImageJsonLd(photo)} />
{:else}
	<PhotoHead
		title="Hillview - annotated panoramas"
		description="Photos from places where cars can't go"
		ogType="website"
		ogImage={{ url: `${HILLVIEW_BASE_URL}/og-card.png`, width: 1200, height: 630 }}
		canonicalUrl={`${HILLVIEW_BASE_URL}/`}
	/>
{/if}

{#if browser && Main}
	<svelte:component this={Main} />
{:else}
	<!-- SSR / pre-hydration placeholder: gives crawlers an h1 + topical sentence
	     and users a branded loading state instead of a blank viewport. Replaced
	     by the app as soon as Main's dynamic import resolves. -->
	<div class="loading-hero">
		<h1>Hillview</h1>
		<p>Annotated panoramas — <a href="/bestof">browse the best views</a></p>
		<p class="loading-note">Loading map…</p>
	</div>
{/if}

<style>
	.loading-hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		height: 100vh;
		text-align: center;
		padding: 1rem;
		gap: 0.25rem;
	}

	.loading-hero h1 {
		font-size: 1.5rem;
		font-weight: 600;
		color: #1f2937;
		margin: 0;
	}

	.loading-hero p {
		margin: 0;
		color: #6b7280;
		font-size: 0.9rem;
	}

	.loading-hero a {
		color: #3b82f6;
		text-decoration: underline;
	}

	.loading-note {
		font-size: 0.75rem;
		opacity: 0.7;
	}
</style>
