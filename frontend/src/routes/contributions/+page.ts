// Client-only personal contributions dashboard: the caller's own annotation
// history, aggregated across every photo. Auth-gated and runtime-only, so never
// prerender (there is no user at build time).
export const prerender = false;
